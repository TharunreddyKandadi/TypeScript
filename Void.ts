function add(a:number,b:number):void{
    let c=a+b;
}
console.log(add(1,3));
//it return nothing(void)  it is used in order we are not using return 


function names(a:string):void{
    console.log(`Hello ${a}`);
}
names('tharun');
//the above code not haveing any return statment so, we can use void;